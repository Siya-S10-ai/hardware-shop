/**
 * Mock share tokens only.
 * These values are not production credentials and must never be treated as authentication.
 */
export const MOCK_SHARE_TOKENS = {
  valid: 'mock_valid_hs_7f3a',
  expired: 'mock_expired_hs_9c21',
  invalid: 'mock_invalid_hs_0000',
}

export const MOCK_TOKEN_PREFIX_LIVE = 'mock_live_'
